# --- AVF Director-layer tooling ---
google-genai
python-dotenv

# --- cheapsawari backend (Slice 1: fare-fetch seam) ---
fastapi
uvicorn[standard]
httpx
pydantic

# --- cheapsawari cloud store (Slice 3b: Firestore) ---
google-cloud-firestore

# --- cheapsawari auth + admin (Slice 6: Google Identity Services) ---
google-auth          # verifies Google ID tokens (offline JWT signature + audience check)
itsdangerous         # signs the Starlette SessionMiddleware cookie
