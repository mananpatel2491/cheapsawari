meta {
  name: Ownership · sign back in as owner
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/auth/dev
  body: json
}

body:json {
  {
    "email": "{{adminEmail}}"
  }
}

assert {
  res.status: eq 200
  res.body.is_admin: eq true
}
